{
  "name": "dylan-video-studio",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "remotion",
    "shared"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm install --workspaces",
    "typecheck": "tsc -p tsconfig.base.json --noEmit"
  }
}
